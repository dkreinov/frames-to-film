"""Backend adapters for external video-generation vendors.

Currently holds the Phase 5 Veo (Gemini Pro web) adapter skeleton.
Future vendors (Kling, Minimax) live alongside `veo_web.py` with the
same interface shape.
"""
