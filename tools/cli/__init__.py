"""Operator-facing CLI wrappers around backend services.

These scripts give the operator a one-command path through each pipeline
stage without needing to spin up the FastAPI server. Useful for service-
stage delivery: cp _template, drop photos, run CLIs, get mp4.
"""
